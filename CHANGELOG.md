# Changelog

## Unreleased

### 2026-08-30 - added a protected Jupyter investigation workflow

- Embedded the 1364x720 systems-console header directly in the notebook's first Markdown cell as a standard `image/png` attachment. The self-contained notebook no longer depends on a neighboring image path; the decoded attachment is byte-identical to the former standalone PNG at SHA-256 `6F6E5F48909A1C843166F91567DC2B3DA2622AC5AF64A4E974CBAB662320FC09`.
- Added `hunt-notebook/Cyber-Defense-Workshop.ipynb`, a guided Python/KQL lab containing all 19 executable, pivot-driven hunts. It requires a normal Jupyter/IPython kernel but uses a standard-library Kusto REST client instead of an additional Kusto SDK. Its opening contract now matches the implemented Section 2 workflow: when `127.0.0.1:8080` is not already a healthy local Kustainer route, the notebook can locate `cloudflared`, read instructor-provisioned service-token values from `~/.config/cloudflare/student-access.env`, and start `cloudflared access tcp` with those values in the child-process environment rather than command arguments. It does not install software, create credentials, or manage containers, and it leaves an already-running local Kustainer unchanged.
- Added `docs/jupyter-kusto-cloudflare-connection.md` to document the notebook's complete request path: IPython's kernel-state, custom-magic, and rich-display roles; Section 2 route selection and Cloudflared startup; Section 3's fixed gateway/Kusto/database checks; the literal `%%kql`-to-REST-to-Kustainer call flow; the historical **Cousteau Semantic Language** meaning retained by the `csl` request field; REST response handling; and the difference between gateway-enforced read-only access and direct-local convention.
- Added idempotent, cross-platform `.venv` provisioning for Windows x86-64/ARM64, macOS Intel/Apple Silicon, and Linux. The bootstrap requires Python 3.10 or newer, resolves the platform-specific interpreter and activation command, reuses a valid existing `.venv` without refreshing it, probes `IPython` and `ipykernel` through that environment's own interpreter, and installs only modules that are actually missing. It creates the environment only when `.venv` is absent and registers the named workshop kernelspec only when it is missing or points to another interpreter; a guarded ready-environment run completed through two read-only probes without invoking venv creation, pip installation, or kernelspec registration.
- Put a green **Key takeaway** callout before each of the 19 hunts to explain the security significance behind the ATT&CK technique rather than reducing the exercise to finding a suspicious row. Each adjacent **Automatic correlation path** identifies the behavior-first seed, the exact identity, source, session, token, policy, report, resource, or object values carried into the next telemetry table, and why those values make the attribution defensible.
- Applied one semantic color system across all 19 TTP narratives and their rendered results: blue `#58a6ff` identifies the header's live endpoint value; green `#3fb950` identifies hunt ranges, hunt headings, and TTP IDs; purple `#bc8cff` identifies telemetry tables in **Tables touched**, every **Evidence path**, and live result routes; and the PowerShell-label warm amber `#f0a45d` identifies `%%kql`, escaped `<placeholders>`, required pivot fields, and the values carried through each seed, first join, and final join. The result renderer uses the same palette while retaining blue pivot stages and a green terminal evidence stage, so formatting reinforces how each value advances the investigation.
- Made the KQL results teach the complete investigation chain. Every `%%kql` hunt returns the shared `HuntId`, `Step`, `Stage`, `SourceTable`, `NextTable`, `PivotFields`, `PivotValues`, `HelpfulInfo`, and `Evidence` contract; the notebook orders those rows by step and renders blue pivot blocks containing the table route, required fields, live values, and a **Why it matters** explanation. Only the terminal stage becomes a green **FINAL EVIDENCE** block, so the student sees how one or two `inner` joins transform TTP behavior into the next query's inputs and ultimately reach the flag-bearing evidence without searching for a flag literal.
- Kept every notebook request on `http://127.0.0.1:8080` and added a single `CONTAINER_IS_LOCAL` decision. A status-only `.show cluster` probe sets it to `True` for local Kustainer HTTP 200 and `False` for existing read-only-gateway HTTP 403; both paths require Kusto ping and target-database visibility, while the nonlocal path additionally requires gateway `/healthz`. A disposable full-notebook run executed all 19 raw `%%kql` hunts against the existing local container; every hunt returned exactly one flag-bearing `Evidence` row, and the committed notebook contains no outputs.
- Removed an accidentally embedded live Cloudflare Service Token ID and secret from the student setup guide and verified zero current tracked-file matches. The guide now uses `Start-StudentAdxProxy.ps1`, which keeps the secret out of command arguments and process listings. The secret remains recoverable from Git history, so it must be rotated again before distributing the notebook or lab files. The automated rotation path was attempted but Cloudflare rejected the locally stored API credential with HTTP 401; no runtime resource or credential changed during that failed attempt.

### 2026-08-29 - upgraded Kustainer while retaining the database and edge path

- Updated only `cyber-conf-wiesbaden-kusto` from engine `1.0.9697.27504` (`2026.07.20.1506-2629-8bf4dbb-master`, source revision `8bf4dbbbe157c779ecc52548179d557b66902ef8`) to engine `1.0.9735.29185` (`2026.08.27.1605-2634-1322d52-master`, source revision `1322d52007a090d75e4bcf68ed5f2769414ec1d5`). The original image is Microsoft's Linux Kusto emulator at [`mcr.microsoft.com/azuredataexplorer/kustainer-linux:latest`](https://mcr.microsoft.com/en-us/artifact/mar/azuredataexplorer/kustainer-linux/about), using the `latest` tag supported by the [official emulator installation guide](https://learn.microsoft.com/azure/data-explorer/kusto-emulator-install).
- Recorded immutable OCI identities for the pulled Linux/AMD64 image: index `sha256:5c45199614a7c9ac7826583f62987d4bbd9da94552090122054eff1c3d333f8c`, platform manifest `sha256:e07f391e28302dcdc130ae470571579461bce08793df769ab4c85eb186bc4050`, and config `sha256:0f3c35c3c5bafa64fe2538af63e6e3a858af12737641eea36dfff915f649dc9c`. The retained, gitignored `data/backups/local-kusto/kustainer-linux-2026-08-27.tar` is a 1,193,096,192-byte `docker image save` archive of that exact pull; it hashed to MD5 `6DED6F204AF1D651A7F5565838CFED60`, SHA-1 `B8467E90555F062EB03447D052C73C1D658796E9`, and SHA-256 `709224EFF2FF5D4DC34CCC3B9A1F9D7D439651A90C8EBADF98039973C18ED780`. MD5 and SHA-1 are archive checksums for operator verification, not registry trust identities; the OCI SHA-256 digests are authoritative.
- Retained `CyberDefendStudentSnapshot` through a clean rebuild rather than attempting to attach state written by another container. Before replacement, the verified 4.33 GiB `data/backups/local-kusto/CyberDefendStudentSnapshot-pre-kustainer-upgrade-20260829.tar` captured the stopped database state, 79 NDJSON payloads, 79 schemas, table/scenario manifests, and restore metadata at SHA-256 `3956A6D920B2AE19772AB6DA8AABBC645B99825AD85C0042B51C125ABFA263AE`; the original 3.66 GB state directory also remains in the ignored backup area for rollback. The August image first restored the complete payload in a disposable rehearsal and retained it across a same-container stop/start, then production restored and reconciled all 79 tables and 637,370 rows with all 19 TTP chains passing.
- Preserved the edge around Kusto instead of recreating the Compose project. The read-only gateway, default-database cleaner, Cloudflare connector, and both Docker networks kept the same container/network IDs, images, IP addresses, and restart counts. Gateway policy still allows `.show tables` with HTTP 200 and rejects `.show queries` with HTTP 403; network isolation still prevents the connector from bypassing the gateway; the existing tunnel stayed up with four active HA connections across Frankfurt edges and zero request errors during the migration.
- Microsoft does not publish Kustainer build-specific release notes or an Azure Updates entry for this image, so no unverified feature claim is attached to the build change. What is verified here is the newer Kusto engine/build revision, successful management and query APIs, complete ingestion and reconciliation, all 19 multi-table TTP queries, persistent same-container restart behavior, gateway compatibility, and Cloudflare tunnel continuity.
- Kept both infrastructure images intentionally current in `compose.yaml`: Kustainer continues to default to `mcr.microsoft.com/azuredataexplorer/kustainer-linux:latest`, and Cloudflare changed from the fixed `cloudflare/cloudflared:2026.7.2` tag to `cloudflare/cloudflared:latest`. The connector was then upgraded to `2026.8.2` (image/index `sha256:0aa26e284f05e6c77ae375b8c9c11d9eb6a448fb7bcd8d40f31cb6176189eb38`) without recreating Kusto, the gateway, cleaner, or either Docker network. Cloudflare identifies `2026.8.2` as the upgrade target for the trailing-slash regression in `2026.8.0` and encoded-path normalization regression in `2026.8.1`; the replacement rejoined the existing tunnel with four HA connections, four Frankfurt edge locations, zero request errors, and a fully READY workshop stack.

### 2026-08-29 - hardened TTP correlations and validation

- Refreshed the README's ADX SOC Overview screenshot to show the current dashboard navigation and 14-day workshop metrics while reducing the image asset from 518,774 to 123,046 bytes.
- Expanded the trainee hunt-query header into a purpose-led guide that documents all 19 hunts, the 3/10/6 email-identity-application category split, the seven canonical and 12 extension hunts, the behavior-first pivot workflow, and a direct link to the complete Cyber Range design and ATT&CK mapping.
- Replaced the synthetic-only cross-tenant application join with the shared Graph and CloudAppEvents `ReportId`, while keeping the acted-on file in `ObjectId`. For SharePoint collection, kept Graph `ReportId` event-specific and correlated the file ID from `RequestUri` to CloudAppEvents `ObjectId` and OfficeActivity `OfficeObjectId`. Conditional Access evidence now uses an array of policy objects and expands `Policy.id` instead of comparing a dynamic list to a scalar.
- Correlated the inbox-rule interactive and non-interactive sign-ins by their shared, non-empty `SessionId` while retaining distinct request-scoped `CorrelationId` values; both sides reject blank session keys so unrelated ambient rows cannot join. Normalized Azure Run Command ARM resource names against Defender endpoint FQDNs with case-insensitive keys, emitted valid provider/type ARM resource IDs, and constrained cloud enumeration to three providers within a two-minute span in one five-minute bucket.
- Bounded directory-reconnaissance correlation to the Graph CLI application, a non-empty token, and the five-minute window after the exact privileged-enumeration event. This prevents repeated account/IP combinations in the full ambient dataset from multiplying one intended Graph evidence row into many results.
- Made instructor Query 01A count every flag occurrence with `extract_all` and `mv-expand`, including duplicate flags in one field. The regex now captures the complete flag token, which is required by Kusto's `extract_all` implementation. Per-field and global totals distinguish missing, misplaced, duplicate, and uniquely placed flags across all declared evidence fields; the validator separately scans every generated top-level field.
- Strengthened the static trainee contract to require the exact matrix table order and a final `Evidence` projection from the declared flag column. The focused generation harness now requires exactly 113 rows, directly proves the inbox-rule chain uses one shared non-empty `SessionId` with distinct non-empty request `CorrelationId` values, compares the complete ordered seven-object scenario narrative, and validates its freshly generated summary rather than the tracked default.
- Made the parallel runner own every child process through completion: partial launch failures and interruptions stop the entire worker tree, wait for exit, dispose process handles, and retain the incomplete-run logs for diagnosis. An injected second-worker launch failure verified that the already-started worker was terminated and no matching process remained.
- Verified the 113-row fixture in an isolated Kustainer database: all 19 executable TTP contracts passed and Query 01A returned 19 `PresentOnce` rows. The database registration, persistent state, and container fixture were removed afterward without touching `CyberDefendStudentSnapshot`.
- Regenerated the 11 affected production tables through the parallel wrapper with normal row overrides intact: 11/11 tables, 109,196 rows, and `DeviceProcessEvents` at exactly 32,000. The complete 79-file payload passes exact flag placement, package validation, and repository safety checks.

### 2026-08-28 - added pivot-driven TTP cyber-range flags

- Converted slides 16-39 of the local cloud threat-hunting deck with MarkItDown and preserved both a generated Markdown extract and a reviewed, categorized TTP catalog. The source `TTPs/` authoring directory is now ignored.
- Implemented all 19 researched email, identity, and application challenges. Every challenge begins with flag-free behavioral evidence and requires one or two cross-table pivots before its unique themed `FLAG:{...}` appears exactly once in the declared final table and column.
- Designated exactly seven challenges as the canonical scenario arc and retained the other 12 as extension hunts over the same dataset. The tracked scenario summary now records the seven IDs, titles, ATT&CK techniques, offsets, and evidence paths.
- Added a machine-readable TTP/table/flag matrix, sequential trainee KQL with no flag literals, deterministic challenge telemetry, and validators for category coverage, schema alignment, pivot depth, unique flag placement, instructor-answer parity, and executable multi-table Kusto results.
- Added a focused 11-table generation harness that reports explicit generator and validator exit codes and counts NDJSON records correctly. The parallel wrapper now accepts comma-separated table filters, can disable production row overrides for zero-ambient fixtures, and supports wider progress rendering.
- Expanded every trainee TTP comment block with a key takeaway, the reason each correlation value must feed the next query, the resulting end-to-end attack story, MITRE ATT&CK mappings, and supporting MITRE/Microsoft references.
- Rethemed the TTP 1 external forwarding destination as `archive@threat-actor.diaries.cn` across its two telemetry records, matrix, trainee narrative, reviewed story, and generated scenario summary.

### 2026-08-07 - documented live CISA KEV JSON enrichment

- **Added a reusable CISA KEV JSON query.** `docs/cisa-kev-json.kql` queries CISA's current Known Exploited Vulnerabilities catalog through `externaldata`, expands its `vulnerabilities` array, and returns the same columns as the CSV feed without persisting data in the workshop database. The root README now describes and links to the capability.

### 2026-08-06 - allowed `externaldata` through the student ADX gateway

- **Permitted the `externaldata` operator and nothing else.** Students can now query public enrichment feeds such as CISA KEV through the read-only gateway. The gateway continues to reject all management commands, `evaluate` request/code-execution plugins, and `cluster()` cross-cluster pivots; the focused regression test covers both sides of that boundary.

### 2026-07-27 — the dashboard is presented to students, not assembled by them

- **Removed the dashboard build from the student guide.** The guide walked students through importing a JSON, editing the `Cyber Defense Workshop ADX` data source, replacing the cluster address, and re-checking the tunnel before any tile would render — setup work that has nothing to do with learning KQL, and that fails in a room of twenty in ways only the instructor can fix. The section is now orientation: what the dashboard shows, how to read it, and the same four triage callouts.
- **Kept the part that was actually teaching.** The four page walkthroughs and their triage callouts were the value in that section and are unchanged. What went is the build instructions, the data-source reconfiguration, and the "you imported the wrong variant" warning — a failure mode students can no longer have.
- **Moved the two dashboard exports to `dashboards/` rather than deleting them.** They were asked to be removed from `STUDENT-GUIDES/`, and they are; but deleting them outright would have broken the instructor's own workflow, since the README and the instructor checklist both import `dashboard-CYBER-DEFEND-V4.json`, and `New-WorkshopDashboardVariant.ps1`, `Sync-WorkshopStudentDashboard.ps1` and `Test-WorkshopDashboardParity.ps1` all read it by default. The schema-76 export is also the only one that imports cleanly into a current portal. Moved with `git mv` so history follows the file.
- **Repointed everything that referenced the old location** — both instructor checklists, the README import step, all three scripts, and the parity failure message that told the reader to re-export "what students import". Verified rather than assumed: `Test-WorkshopDashboardParity.ps1` resolves the moved files and passes 40 of 40 tile titles and 40 of 40 tile queries across all three copies.
- **Left the two dated reports alone.** `REPO-EVALUATION-2026-07-26.md` and the earlier changelog entry still name the old path, because they are records of what was true on their date and rewriting them would make them lie.

### 2026-07-27 — the workspace name was in the telemetry too, and only a wider scan could see it

- **`AgentsInfo.DeclaredDataSources` carried the real Log Analytics workspace name on 1,384 rows.** Two hardcoded catalog entries emitted `Sentinel: <workspace>` into generated telemetry. They now emit `Sentinel: USAG-Wiesbaden-SOC`, which is what the rest of that catalog already looked like — every neighbouring value is fictional (`usag-cyber.local`, `partner.example`, `USAG Cyber Defense`), so the real name was the single non-fictional string in it.
- **It was found only because the scan got wider, not because the reasoning got better.** The previous payload check searched one token, the tenant name. This one searches the workspace and cluster names as well, and found a leak that the narrower scan was structurally incapable of seeing — the same failure mode as the `Hierarchy` leak the day's earlier entry describes, one token over. The check that finds a leak is the one that looks for the value, not the one that confirms yesterday's fix still holds.
- **Drew the line at what reaches a student, not at the string itself.** The workspace and cluster names also appear in a default `-ClusterName`, a default `-WorkspaceName`, and the `clusterUri` of both dashboards — all of which point at infrastructure students genuinely connect to, so renaming them breaks the workshop rather than protecting anything. The rule applied here is that a value emitted into telemetry gets sanitised, while a functional default or a source comment stays. The three remaining mentions in comments are left alone deliberately.
- **Verified with a database-wide search rather than a per-table one.** A full regeneration across 8 workers (19.42 min, 79 tables, 637,370 rows) followed by a pinned single-table rerun of `AgentsInfo` — same end time and seed, reproducing at exactly 5,535 rows — leaves the payload at 79 files and 637,370 rows with zero hits for any of the three names. Re-imported and reconciled to the same 637,370, and `search` across every table in the database returns 0, 0 and 0. Readiness READY, safety CLEAN at 166 tracked files.

### 2026-07-27 — the tenant name was also hardcoded, on every row of a table nobody had scanned

- **Rethemed the replacement tenant from `contoso` to `usag-wiesbaden-cysm27`.** The sanitiser was rewriting captured `*.onmicrosoft.com` hosts to Microsoft's example tenant, which is safe but reads like a different exercise; `autodiscover.usag-wiesbaden-cysm27.onmicrosoft.com` belongs to the scenario the workshop actually tells. The workshop tenant had to be added to the example-tenant allowlist in both `New-SyntheticTelemetry.ps1` and `Test-TrackedContentSafety.ps1` — without that, the rewrite is not idempotent and a second application would rewrite the workshop's own tenant, and the gate would flag the generator's source for containing the string it deliberately emits.
- **Found a third instance of the leak class, in the one place the previous two fixes could not reach: a hardcoded literal.** `AzureActivity.Hierarchy` was built as `'{0}/<tenant>' -f $tenantId`, putting the real tenant's display name on **all 7,936 rows** of that table — not three rows in a corner, every row a student sees. The GUID beside it is fine: it is documented as a fixed random value and is not the real tenant id.
- **Three gates and my own verification all passed while it shipped, for four different reasons.** It reaches no field profile, so capture-time suppression never applies and `Test-FieldProfileSafety.ps1` cannot see it. It is not a captured value, so `ConvertTo-WorkshopSafeCapturedValue` — the fix for the previous two — never touches it. `Test-TrackedContentSafety.ps1` matches the **domain** form, so a bare tenant name in a tracked file is clean. And my end-to-end check queried `DeviceNetworkEvents.RemoteUrl`, the table I had just fixed, which confirms a fix rather than searching for the problem. What surfaced it was scanning the whole payload for the bare name instead of the domain — the measurement, not the reasoning.
- **Deliberately did not add the bare name as a gate indicator.** That is the obvious hardening and it is the wrong one here: this scanner lives in a public repository, and `Test-TrackedContentSafety.ps1` already stores its baseline as SHA-256 hashes precisely because "a baseline holding literal identifiers would become the leak it exists to prevent." Hardcoding the tenant name into the scanner would republish the string just removed. The regression guard that would work is a hashed forbidden-token list; it is not built yet.
- **Left the workspace and cluster names alone, deliberately.** The same bare-name class covers `DIBSecCom` and `dibsecadx` across 22 files, but several are load-bearing rather than prose — a default `-ClusterName`, a default `-WorkspaceName`, and the `clusterUri` in both the instructor and student dashboards, which point at a cluster students genuinely connect to. Replacing those is an infrastructure rename that breaks the workshop, not a sanitisation.
- **Renamed the two misnamed `sample/` exports** to `-GeneratedExport.csv` and pointed `$SampleAlias` and the two profiles' `sources` at the new names, so the next provenance question is not answered by a filename that lies. This surfaced a separate problem left unfixed: those two profiles are **circular**, grounded on the generator's own output rather than on a capture.
- **Sanitised the Linux TVM inventory read path**, which is read by the same unprofiled `Import-Csv` route. Measured first: no indicator fires on any of its 664 rows today, so this is a structural guard against the next capture dropped into that path, not a live leak.
- **Verified end to end.** Regenerated `AzureActivity` alone, pinned to the same `-TelemetryEndTime` and `-RandomSeed 1702` as the other 78 tables so the estate stays in lockstep, and it reproduced at exactly 7,936 rows. The payload holds 79 files and 637,370 rows with **zero** occurrences of the tenant name, re-imported and reconciled to the same 637,370, and the database now answers `5867d47f-…/usag-wiesbaden-cysm27` on `Hierarchy`, `autodiscover.usag-wiesbaden-cysm27.onmicrosoft.com` on `DeviceNetworkEvents`, and zero on a `has` search for the tenant name across four tables. Readiness READY, safety CLEAN at 166 tracked files and 69 profiles.
- **Correcting the timing claim I made when switching generators.** I presented the 8-worker parallel path as the faster one. Measured on this machine it took **20.20 min** against roughly **17 min** serial, and the wrapper's own docstring claims 13–14. The reason to use it is not speed — it is that it pins the end time and seed once and forwards them to every worker, which is what makes a single-table regeneration agree with the other 78.

### 2026-07-27 — a second tenant domain in the same unprofiled path, and a 25,000-identifier scare that was not one

- **Closed the rest of the leak in the captured-value path.** Scanning the regenerated payload for structural indicators rather than for one known string turned up `autodiscover.<tenant>.onmicrosoft.com` on three `DeviceNetworkEvents` rows — the real tenant domain, arriving through the same `Import-Csv` route as the workspace id and missed by the first fix because that fix only rewrote GUIDs. `ConvertTo-WorkshopSafeCapturedValue` now also rewrites the tenant label of any `*.onmicrosoft.com` host to `contoso`, leaving Microsoft's documented example tenants (`contoso`, `fabrikam`, `northwind`, and the rest) alone so the student instructions keep saying what they are supposed to say.
- **Verified the sanitiser against its inputs instead of inferring it from the diff:** `autodiscover.<tenant>.onmicrosoft.com` → `autodiscover.contoso.onmicrosoft.com`, `contoso.onmicrosoft.com` unchanged, `login.microsoftonline.com` untouched, embedded GUIDs remapped, and the same input produces the same output on repeated calls — which it must, because eight worker processes have to agree.
- **I raised an alarm about 25,429 leaked tenant GUIDs. It was a false positive, and the measurement was the problem.** Comparing every GUID in `data/generated` against every GUID in `sample/` showed 25,435 shared values, concentrated in `OfficeActivity` and `AADSpnSignInEventsBeta`, in per-event columns — `CorrelationId`, `RequestId`, `UniqueTokenId`, `SourceRecordId`, `MailboxGuid` — at one distinct value per row. Per-event identifiers cannot be a shared Microsoft vocabulary, so the reading was that real telemetry was being copied wholesale.
- **What the comparison actually assumed was that everything in `sample/` is a tenant capture. Two of those files are not.** `sample/OfficeActivity-RealTelemetry.csv` contains this repository's own literal `$RestApiTenantId$` placeholder 18,262 times and 13,472 addresses from the generator's hardcoded `45.23.0.0` block. `sample/AADSpnSignInEventsBeta-RealTelemetry.csv` contains all 141 application ids that can be recomputed from `$servicePrincipalNames` with `New-StableGuid`, plus invented service principals like `Workshop Managed Identity` and `USAG Cyber Sync Helper`. Both are exports of generated output that were saved back into `sample/` under a `-RealTelemetry` name. The overlap was synthetic matching synthetic; the two files account for 25,415 of the 25,435.
- **The tell was arithmetic I nearly walked past.** The `AADSpnSignInEventsBeta` capture holds 9,301 distinct GUIDs, yet three columns each reported 7,025 distinct matches inside it. Three disjoint sets of that size do not fit, so the columns had to be carrying one shared value per row — which is exactly what the generator does, and pointed at the generator rather than at the capture.
- **`DeviceNetworkEvents-Real.csv` is a genuine capture and the leak through it was genuine.** The distinction matters: the fix belongs where a real capture is read, not everywhere a `-RealTelemetry` filename appears.
- **Left alone deliberately:** the low-cardinality overlaps that remain — 14 `ResourceId`, 141 `ApplicationId`, 282 `ServicePrincipalId` — are all recomputable from tracked catalogs in the generator, so they are synthetic by construction and there is nothing to scrub.
- **Verified end to end, in the database students actually query.** Regenerated 79 tables and 637,370 rows, re-imported, and reconciled to the same 637,370. `DeviceNetworkEvents` now answers `autodiscover.contoso.onmicrosoft.com` on the three rows that previously carried the live tenant, the ingestion endpoint reads as a hashed identifier rather than the real workspace id, and a `has '<tenant>'` search across the table returns zero rows. Repository safety is clean at 166 tracked files, and the readiness gate reports READY with its expectation derived from the payload rather than a pinned constant.
- **Still outstanding:** the misnamed `sample/` exports should be renamed so the next person reading a provenance question is not misled the way this scan was, and `sample/export-tvm-machine-software-inventory-linux.csv` is read by the same unprofiled route and is not yet passed through the sanitiser.

### 2026-07-27 — the generator was copying raw tenant telemetry straight through

- **Corrected a claim I made in an earlier entry and had already committed.** I wrote that the live workspace id in `DeviceNetworkEvents` was a stale artifact that "a regeneration and re-import clears". A full 13.6-minute regeneration disproved it: 79 tables, 637,370 rows, and the identifier reproduced byte for byte in the same file. The reasoning behind the claim — it is in no field profile, and `grep` finds it in no script — was true and still did not support the conclusion, because it assumed the profiles are the only way telemetry reaches a generated row.
- **They are not.** `Import-WorkshopDeviceNetworkEventProfileCatalog` calls `Import-Csv` directly on `sample/DeviceNetworkEvents-Real.csv` — raw tenant capture — and copies `RemoteUrl` and `RemoteIP` verbatim onto generated events. That path never touches `Export-WorkshopTelemetryProfiles.ps1`, so profile suppression never applies, and `Test-FieldProfileSafety.ps1` cannot see it either: it audits profiles, and this value was never in one. Both safety gates were clean while the leak shipped in the payload students query. The identifier was live only because the capture was live; anything else in those columns would have travelled the same way.
- **Sanitised at the boundary rather than by blacklisting the string.** `ConvertTo-WorkshopSafeCapturedValue` rewrites any GUID embedded in a captured value through `New-StableGuid`, so `<workspace-id>.ods.opinsights.azure.com` becomes `7b3aa118-….ods.opinsights.azure.com`. The substitution is seeded, not random, because eight worker processes must agree on it for generation to stay reproducible. Applied to the DeviceNetworkEvents catalog and the DeviceInfo catalog, which reads the same way.
- **Kept the hostname deliberately.** Only the identifier is replaced, not `ods.opinsights.azure.com` — that is a Microsoft service domain, and Log Analytics ingestion traffic is exactly the kind of egress the exercise is meant to teach students to recognise. Removing it would have cost the lesson to fix a problem that was never in the domain name.

### 2026-07-27 — three operational faults that only surface mid-class

- **The documented student-path health check was a command the gateway refuses by design.** Three documents told a co-instructor to run `.show cluster` and treat anything but `200` as a broken proxy or credential. It is deliberately not on the gateway allowlist and answers `403` on a perfectly healthy stack, so the documented diagnostic sends you rotating credentials at exactly the wrong moment. All four student-path probes now use `.show databases`, which is allowlisted, with a note saying why.
  - **One of the five reported locations was not a bug.** `IaC-CFT-TF-Setup.md:95` runs immediately after `docker compose up` against `127.0.0.1:8080` on the instructor host, and the gateway publishes no host port — that request reaches Kustainer directly, where `.show cluster` genuinely returns `200`. Measured both paths rather than trusting the report: direct `200`, through the gateway `403`. Changing it would have replaced a correct instruction with a worse one.
- **The readiness gate no longer carries a row count that can go stale.** It now derives the expected tables and rows from the NDJSON in `data/generated`, which is what `Import-GeneratedDataToKustainer.ps1` reconciles against and therefore the only figure that can be correct by definition. The pinned constants remain as a fallback for a fresh clone, where the payload is gitignored and absent, and the run reports which source it used. Regenerating on site now moves the expectation with the data instead of stranding the one command that decides go/no-go.
- **Fixed a client timeout shorter than the server timeout in the helper that drives every ingest, `.clear table data`, `.drop table` and ADLS export.** The client gave up at a flat 300s while callers ask ADX for up to **1800s** — worse than the 600s the report described. Anything between the two aborts client-side while the server carries on and finishes, so the run fails, the operator reruns it, and the rerun lands on top of whatever the first attempt already committed. The client budget is now the server's plus a margin, so the client always outlasts the server rather than the reverse.
- **Removed a blind retry that could silently duplicate an ingested batch.** `-MaximumRetryCount 3` retries on any 4xx or 5xx, and a 5xx from a load balancer *after* an `.ingest inline` has committed means the rows are already in — replaying it duplicates them, and `Initialize-Workshop` still prints success. Auto-retry is now opt-in per call via `-Idempotent`, off by default, and only for `429`/`503`, honouring `Retry-After`. The read-only callers that genuinely benefit — the `.show operations` polling loops, the schema reads, the table-existence check — opt in, so nothing loses resilience where replay is safe.
- Verified behaviourally rather than by reading the diff: a stub server proves non-idempotent calls issue exactly one request and never retry a `429` or a `500`, an idempotent call retries `429,429,200` in three requests and succeeds, and a `500` is not retried even when idempotent. 8/8 including static checks. The safety gate and the readiness preflight both still pass.

### 2026-07-27 — the safety gate now looks where the identifiers actually were

- Extended `Test-RepositorySafety.ps1` with a third rule that scans **every tracked text file** — `.ps1`, `.psm1`, `.md`, `.json`, `.bicep`, `.mjs`, `.kql` and the rest — for live tenant identifiers. Rules 1 and 2 covered forbidden paths and `metadata/field-profiles/`, which is why the tenant GUID, both subscriptions, a managed-identity object id and the Log Analytics workspace id sat undisturbed in 21 tracked files: they were in the one place nothing looked. The hook and CI both call this script, so the new rule is enforced in both without a second copy of anything.
- **Two rules, because they need different treatment.** Structural indicators — a subscription or tenant resource path, a portal deep link carrying a tenant, an onmicrosoft domain, a bearer token or JWT, a Log Analytics ingestion endpoint — are unambiguous and fail outright. Bare GUIDs are not: the repository legitimately holds about 150, mostly dashboard tile ids and Microsoft well-known first-party application ids. Failing on all of them would make the gate noise, and a noisy gate gets bypassed, so known GUIDs are baselined and only new ones fail. That is the shape that matters — the DIBSecCom workspace id was a new GUID once.
- **The baseline stores SHA-256 hashes, not values.** A baseline holding literal identifiers would become the leak it exists to prevent. The failure message names the file and line instead, so review happens at the source rather than in a list of secrets.
- **The first draft of the structural rules was wrong and the run said so.** A bare `/resourceGroups/` fired 14 times on the generator's own synthetic resource paths, `contoso.onmicrosoft.com` is exactly what the student instructions are supposed to say, and the scanner flagged its own regex definitions in `WorkshopSensitiveContent.ps1`. What makes a path dangerous is the GUID in it, so every structural pattern now requires one; Microsoft's documented example domains are exempt; and the three safety scripts are excluded because they must contain the patterns as source. Structural findings went from 27 false positives to zero.
- **Fixed an early exit that would have silently skipped the new rule.** In staged mode the script returned success as soon as it found no field profiles staged — which is the common case, since most commits touch no profiles at all. Wired in naively, rule 3 would have run almost never and still reported "passed".
- Verified against seven deliberate leaks pushed through the real pre-commit path: a brand new bare GUID, a subscription resource path, a portal tenant deep link, a real onmicrosoft domain, a Log Analytics ingestion endpoint and a bearer token are each blocked, while a control line holding `contoso.onmicrosoft.com` and a baselined GUID passes. 7/7, and the whole tree is clean at 166 files and 151 baselined hashes.

### 2026-07-27 — old telemetry had been baked into the tracked schemas

- Removed live tenant identifiers from **23 tracked files**: the DIB Security tenant GUID, both subscription GUIDs, the `uami-adx-backup` object id, the DIBSecCom Log Analytics workspace id, the backup storage account name, and four portal deep links that carried the tenant in the URL fragment. The repository teaches that embedded tenant identifiers are radioactive and enforces it with a pre-commit hook — for field profiles only, so these sat in precisely the blind spot the gate does not cover.
- **The reported location was three files. It was twenty-one.** The three named in the report were real, but the bulk of it — 17 of the 21 files carrying a real GUID — was under `schemas/`, which nobody had looked at. Counting properly also corrected my own first pass: 113 occurrences rather than 117, because `dibsecadx` is a substring of the storage account `adxdibsecadx09da4d6a` and a naive scan bills the same characters twice.
- **Fixed the source rather than the symptom.** `tools/Build-SchemaFromLiveTable.ps1` writes `"Most common: <observed value>"` into every column description, straight from live tenant telemetry. That one line is how all 17 schema files were populated, and scrubbing them without touching it would have re-introduced everything on the next capture. The builder now drops any sample that trips a tenant-data indicator and keeps the cardinality, which is the part of the description anyone actually reads. The scrub applies exactly that rule to the files written before it existed, so re-running the builder now converges instead of fighting.
- Indicators moved into `scripts/WorkshopSensitiveContent.ps1` and are shared by the builder and `Test-FieldProfileSafety.ps1`. Writing them out twice is how the pre-commit hook and CI ended up enforcing different rules in the 2026-07-26 evaluation, and repeating that here would cost live tenant data in a public repository rather than a confusing CI run.
- Deployment identifiers now come from a gitignored `workshop.settings.json` (or environment variables) through `scripts/WorkshopSettings.ps1`, with `workshop.settings.example.json` tracked as placeholders. **Nothing has a fallback value on purpose** — a default that happens to be someone's real subscription is how this started. `Export-WorkshopTelemetryProfiles.ps1` no longer carries the workspace id as a parameter default and only demands one when it actually queries Log Analytics, so `-LocalOnly` still runs with no configuration at all. The restore script and the private-endpoint Bicep lost their literals the same way.
- Verified: all seven identifiers now return zero hits across the entire tracked tree; 79 schema files still parse; descriptions survive (31/31 columns on Heartbeat); the field-profile gate is clean at 69 profiles and 758 vocabularies and still fails a deliberately poisoned profile; repository safety passes; and workshop package validation — which checks all 79 generated NDJSON files against these schemas — passes.
- **Not done here, deliberately.** The same workspace id is still live in the student-queryable database: `DeviceNetworkEvents | where RemoteUrl contains 'opinsights'` returns 3 rows of `<workspace-id>.ods.opinsights.azure.com`. ~~It is a stale artifact in `data/generated` from a run predating the profile sanitisation — it appears in no field profile and is not produced by the generator — so a regeneration and re-import clears it.~~ That was held back rather than run unattended.
  - **Correction (2026-07-27, later entry below).** The struck sentence is wrong. A regeneration does *not* clear it: the generator reproduced the identifier byte for byte. "Absent from the field profiles" did not mean "absent from the generator" — there is a second, unprofiled path that reads `sample/*.csv` directly. Diagnosed and fixed below.

### 2026-07-27 — the database allowlist was only reading the envelope

- Closed a cross-database read that defeated `KUSTO_ALLOWED_DATABASES` entirely. The allowlist was checked against `payload.db` and never against the query text, and while `cluster(` was on the deny list `database(` was not — so a student connected to the allowlisted database could read any other database on the cluster with `database('SomeOtherDb').SomeTable | take 100` while the envelope still read `CyberDefendStudentSnapshot` and passed. `.show databases` and `.show databases schema` are allowlisted, so the names and schemas needed to aim it were already available. An allowlist that inspects only the envelope is not an allowlist.
- **The existing test suite passed this the whole time, and passed it for the wrong reason.** The only cross-database case tested was `cluster('other.kusto.windows.net').database('x').T`, which the `cluster(` deny catches before `database(` is ever reached. The bare form went straight through, so the coverage read as green while testing a different control.
- Confirmed it on the running gateway before changing anything, because the local evidence is easy to misread: `database('KustoMonitoringPersistentDatabase').Foo` came back **400**, which looks like a refusal until you notice 400 is Kusto's error and gateway denials are 403 with the gateway's own JSON. The gateway had forwarded it; the 400 only meant that database does not exist on a single-database emulator. The query that settles it is `database('CyberDefendStudentSnapshot').AlertInfo | count` — **200, 6,033 rows**. The construct executes, and on the managed-ADX path, where other databases can share the cluster, it would return their rows.
- **Did not simply deny `database(`,** which was the obvious fix and would have broken the class. The managed database name `cyber-defend-usagwsbdn-cys26` contains hyphens, which forces students into `database("...")` syntax, and the web UI qualifies queries the same way. So the names are now parsed out of the query text and every one of them must be on the allowlist — the primitive keeps working for the database the student is entitled to and stops at the boundary for every other. A name that cannot be resolved statically, such as `database(strcat(...))` or `database(toscalar(...))`, is refused rather than guessed at, matching the fail-closed direction the rest of the policy takes.
- Verified red before green rather than trusting a green suite: against the pre-fix module the cross-database read is `ALLOWED`, returns **200**, and reaches the stub cluster; against the fixed module it is **403** and never leaves the gateway, while `database('CyberDefendStudentSnapshot').AlertInfo` still returns 200 in both. On the live gateway all four spellings — single quote, double quote, spaced parens, mixed case — now return 403 with the gateway's wording instead of 400 from Kusto. Suite at 78 tests, and the preflight still reports READY.

### 2026-07-27 — the go/no-go check was failing on correct data

- Fixed `Test-WorkshopReadiness.ps1` asserting **623,832** rows when the dataset has held **637,370** since the parallel-generation work landed the day before. That change verified its own result end to end and is recorded in this file as "637,370 rows across 79 tables"; the preflight's constant was simply never carried along with it. The one command meant to answer *is the workshop ready* has been answering **NOT READY** on a perfectly good database, and the remedy it printed — re-run the importer — would have changed nothing, because the database already equals the payload exactly. A gate that cries wolf and then prescribes a no-op is worse than no gate, since the habit it teaches is to ignore it.
- Settled which number was authoritative by counting the payload rather than trusting either side or the commit history: `data/generated` holds 79 NDJSON files totalling 637,370 lines, matching `metadata/tables.manifest.json` table for table with nothing extra and nothing missing. That is the figure `Import-GeneratedDataToKustainer.ps1` reconciles against, so it is the only one that can be correct by definition. Recorded the command in a comment beside the constant, because the failure mode here was a remembered number drifting from a measurable one.
- Verified red then green: before the change the preflight exited 1 with `FAIL database holds the workshop data / 79 tables, 637,370 rows` as its only failure; after, all checks pass and it exits 0.

### 2026-07-26 — 46 minutes to 14, and the generator is finally reproducible

- Cut the full regeneration from **46.2 minutes to 14.4** (3.2x) by running the 79 independent tables across 8 worker processes. 8 is the measured optimum, not a guess: scaling was 4.7x at 8 workers and only 5.2x at 16, because SMT adds little to CPU-bound PowerShell and every worker re-pays the ~2 minute setup phase, so 16 workers finish a full run *slower* than 8. Memory never binds — a worker peaks at 522 MB.
- Found the generator was **never byte-reproducible across processes**, which quietly contradicted the promise that a fixed `-RandomSeed` reproduces the dataset. Two identical runs produced different files. Three separate causes, none of them the one being looked for:
  - The per-table reseed ran *after* the row-target draw, and with the default `-NormalRowsPerTable` that target is itself a random draw — so a table's **row count** depended on how many tables ran before it. `DeviceImageLoadEvents` came out 5,022 rows in a full run and 8,743 on its own. The reseed comment already said it existed to make output independent of run order; it was one line too late, and fixed the row contents but not the count.
  - The password-spray plan walked `$script:SignInSprayTargets.Keys`. .NET randomises string hashing per process, so a different quarter of the sprayed accounts locked out on every run and the same account gave `50053` in one generation and `50126` in the next. Introduced by the spray work earlier the same day.
  - Nested values are serialized to JSON *text* where they are built, so they never reached any normaliser and carried the hashtable's per-process key order into the file. This is what made 18 tables differ.
- Everything now routes through `ConvertTo-WorkshopStableDynamic` (sorted keys, recursive) and `ConvertTo-WorkshopJsonValue`. Proven: a single table regenerated alone is **byte-identical** to the same table from a full run, two runs in separate processes are identical, and all **79 tables generated in parallel are byte-identical to the serial baseline**. That last check is the one that makes parallelism safe to trust, and it was impossible to make until the determinism was fixed.
- Raised JSON serialization depth to 15 everywhere, including the wrapper default that was sitting at PowerShell's default of 2. Past its depth limit `ConvertTo-Json` does not fail — it substitutes `ToString()`, which is exactly how `System.Collections.Hashtable` gets written into a data file. Scanned the existing dataset: nothing was ever truncated, so this is preventive.
- Guaranteed `DeviceProcessEvents` at 32,000 rows and made it impossible to lose silently. It had two escape routes: passing `-NormalRowsPerTable` switched per-table overrides off entirely, and a worker that never received the override would fall back to the ambient 5,000-10,000. Per-table targets now outrank `-NormalRowsPerTable`, and the generator throws if a table with an explicit target writes the wrong count.
- Parallelised the quality gate across tables (1.59 → 1.07 min), with the report sorted by score *and name* so two runs of the same data produce a diffable report rather than a reshuffled one.
- Added a progress meter to generation, validation and import — bar, count, percent, elapsed and an ETA extrapolated from the observed rate. The generation meter reads the worker logs, because the generator already writes one line per finished table; counting output files instead would report every table as finished the moment its file was created. It redraws in place at a prompt and switches to one line per item when redirected, where in-place redraw would collapse a log into a single enormous line.
- **Tried parallelising the Kustainer import and reverted it.** At 4 concurrent ingests the emulator returned `429 TooManyRequests`, leaving 49 row-count mismatches and a database holding 225,456 of 637,370 rows. The reconciliation step caught it and exited non-zero, which is what it is for. The serial import takes 1.15 minutes, so there was little to win and a broken snapshot to lose.
- Verified end to end: 637,370 rows across 79 tables, `DeviceProcessEvents` at exactly 32,000, identity invariants holding, quality gate green at a 93.9% mean, all smoke tests passing, 40/40 dashboard tiles returning data, and dashboard parity clean.

### 2026-07-26 — moved the telemetry cache off CSV, and guarded the boundary

- Measured the blast radius before acting on it, and the measurement contradicted my own speculation. I had suggested other profiles might hold quietly degraded vocabularies; across all 78 cached captures (342 MB, 127 declared `dynamic` columns) there were **zero** remaining CLR type names and **zero** dynamic columns that failed to parse as JSON. The single apparent hit was my own line-based read breaking on a cell containing newlines — which is itself a fair demonstration of the problem with CSV. **The full re-capture sweep was therefore not done**: it would have churned every tracked profile and reshuffled vocabularies to repair damage that is not there.
- Found the real, total loss where nobody was looking: **type**. `Import-Csv` returns strings, so `$_ -is [int]` was never true and the numeric-stats path was dead for every CSV-sourced profile — **0 of 158** numeric columns carried `numericMin`/`numericMax`/`numericAvg`. Booleans split the same way, 26 columns rendered PowerShell's `True`/`False` against 22 with JSON's `true`/`false` (no column mixes both, so no weights were corrupted — but a column changing source would flip its spelling and read as entirely foreign).
- Switched the capture cache to NDJSON, as an added format rather than a migration. Both exporters write one JSON row per line; the profiler prefers `.ndjson` and falls back to `.csv`, so curated captures and tables with no live source keep working and nothing needs re-capturing. NDJSON is preferred **regardless of file size** — the old "largest capture wins" rule would otherwise favour a big lossy CSV over a smaller faithful one. Proven on `CloudStorageAggregatedEvents`: 7 of 7 numeric columns recovered their statistics, and the PowerShell-cased booleans are gone.
- Guarded the boundary so this cannot ship quietly again. The exporter now refuses to write a profile whose vocabulary is a CLR type name, reports the offending columns, and exits non-zero. Previously the only thing that noticed was the quality gate — three steps and a 46-minute regeneration downstream, by which point the profile on disk already looked valid. Verified against a deliberately flattened capture: refused, exit 1, nothing written.
- Closed a real exposure found while doing it. `.gitignore` only covered `sample/*.csv`, so the NDJSON captures this change introduces would **not** have been ignored — raw tenant telemetry, one `git add -A` away from a public repository, with only the pre-commit hook standing between. The whole `sample/` directory is now ignored, which no longer depends on anyone remembering to add an extension. `Test-RepositorySafety.ps1` already blocked `^sample/` as the second layer, and nothing under `sample/` was ever tracked.
- Left the superseded CSVs on disk. They are gitignored, inert now that `.ndjson` is preferred, and deleting a capture that may be the only copy of a table with no live source is not a decision to take silently.

### 2026-07-26 — the exporter had been destroying its own captures

- Traced the unusable vocabularies to the capture path rather than the profile. Every one of the 2,000 rows in `sample/ExposureGraphEdges-RealTelemetry.csv` held the text `System.Collections.Hashtable`, so regenerating the profile from it would have reproduced the same garbage faithfully. The exporter writes that cache itself with `Export-Csv`, which calls `ToString()` on anything it does not recognise, and then prefers the cache on the next run — so a live capture with real dynamic columns was flattened to CLR type names on the way to disk and read back as the column's entire vocabulary. Naming it as the gate now did would never have fixed it.
- Gave both scripts a shared `ConvertTo-ProfileValueText`. Complex values are rendered as compact JSON — what the column actually holds, what the generator emits, and what survives a CSV round trip — while scalars keep their existing rendering so no profile changes shape for no reason. The exporter flattens rows through it before `Export-Csv`, and the gate compares through it, because `[string]` on an array joins with spaces and on a nested object yields `@{a=b}`; without both sides agreeing, a dynamic column compares its JSON against PowerShell's rendering and every row reads as foreign. Both build their value lists in a loop rather than a pipeline, since emitting an array to the pipeline unrolls it and would scatter one cell across several vocabulary entries.
- Stopped a `json` pattern from suppressing the vocabulary on its own. The pattern tests exist to keep high-cardinality structured identifiers — guids, hashes, paths, timestamps — from being replayed literally, but they also caught the newly-JSON category columns, which left the profile technically valid and the column still unmeasurable. Low-cardinality JSON now carries its vocabulary; the existing cardinality bound still keeps genuine property bags such as `EdgeProperties` (1,136 distinct) and `AdditionalFields` (3,132) out.
- Re-captured the three affected tables from live telemetry. All seven columns now carry real values: `SourceNodeCategories` has 29 observed category tuples, `TargetNodeCategories` 46, `OperationNamesList` 17. `Md5Hashes` turned out to be genuinely empty in production and is now recorded as such.
- Made those columns checkable, which immediately exposed three invented vocabularies that had been hiding behind the broken profile — exactly what the exercise was for. A device is `["compute","device","physical_device"]`, not the `["compute","endpoint"]` the catalog made up; the product says `has permissions to`, `has credentials of` and `routes traffic to`, not `can access`, `has credentials to` and `routes to`; and `EdgeProperties` carried `accessType`/`isDirect` keys that appear nowhere in the real table. The node catalog is now the observed label/category pairing, weighted as observed, and the comment claiming the table times out through the advanced hunting API is gone — it returned 7,000 rows.
- Followed the labels through to the names. Grounding the catalog introduced `Cve`, `subscriptions` and role-assignment nodes, and the old "a user gets a UPN, everything else gets a device name" rule would have produced a CVE node named after a workstation.
- Fixed a single-element array being written as a scalar. `return $Value` unrolls a one-element array, so a dynamic column holding `["ListFiles"]` was written as `"ListFiles"` while multi-element values came through intact — the asymmetry made it look like a vocabulary problem rather than a serialisation one. Dynamic columns are now arrays in the emitted data, which is what `mv-expand` needs.
- Verified: 620,010 rows across 79 tables, quality gate green with **no unusable vocabularies left anywhere in `metadata/field-profiles/`**, identity invariants holding, all smoke tests passing, 40/40 dashboard tiles returning data, and parity clean. The three regenerated profiles carry no subscription ids, GUIDs, addresses or UPNs.

### 2026-07-26 — the quality gate passes, and I had misread why it was failing

- Corrected my own account of the previous entry before fixing anything. I told the maintainer the `SigninLogs` sparseness was entirely pre-existing, having measured ambient-versus-all fill across the whole 8,041-row file, where the 400 spray rows are 5% and change nothing. But `Test-SyntheticDataQuality.ps1` reads the **first 1,500 rows**, not a random sample, and scenario rows sort to the front of the file — so the spray rows were 27% of what the gate actually looked at. Eleven columns were genuinely never populated; roughly thirty more read sparse because of the rows I had just added. Verifying against a convenient whole-file measure instead of the tool's own sampling method turned a regression into a clean bill of health.
- Found the real cause, which was structural rather than a missing list of fields. Every sparse column reported exactly "production 100% → generated 73%", and 400 of 1,500 rows is 26.7%: the columns were fully populated on ambient rows and empty on mine. The profile-driven column fill lives inside `New-WorkshopRecordObject -Ambient`, and `Add-Record` — the path every scenario and evidence row takes — never passed it. Passing `-Ambient` directly was not an option, because it also *clears* the columns production leaves blank, including `IsRisky` and `RiskLevel`, which the spray rows set deliberately and which the dashboard's risky-sign-in tiles depend on.
- Split the switch instead of widening it. `-Blend` applies the observed distributions and the pattern fill, then lets the caller's explicit values win, but skips the empty-column pass that exists so ambient rows do not invent signal. Ambient rows keep `-Ambient` and behave exactly as before; evidence rows that have to hide inside ambient traffic take `-Blend`. `SigninLogs` went from 50.5% to 94.6%, with all 31 sparse columns closed and the spray narrative intact — 360 rows still carrying `IsRisky`, `RiskLevel`, RU geolocation, the three attacker addresses and their failure codes, with zero orphan detections.
- Populated eleven `SigninLogs` columns the generator had never written at all, using the vocabularies the profiles already record: `OperationName` (`Sign-in activity`), `TokenIssuerType` (`AzureAD`), `ClientCredentialType` (`none`), `IncomingTokenType` at the observed 88.4/11.5/0.1 split, plus `TenantId`, `ResourceId`, `UserType`, `UniqueTokenIdentifier`, `CrossTenantAccessType`, `ServicePrincipalName` and `ProcessingTimeInMilliseconds`.
- Gave `IdentityQueryEvents` its real vocabulary. The generator was emitting `LdapSearch`, `Rubeus`, `Search` and port 389 — none of which appear in the profile, and all of which a query written against real telemetry would miss. Domain controllers overwhelmingly answer DNS: the table is 81.3% `DNS query` on port 53 against `_msdcs` records, 18.7% `LDAP query` with the full search-scope string. 72% → 100%. The kerberoasting scenario's own `LdapSearch` row is left alone; it is deliberate hand-authored signal.
- Taught the gate to report a profile bug instead of blaming the generator for it. `ExposureGraphEdges` was failing on three columns whose expected vocabulary is literally `System.Collections.Hashtable` and `System.Object[]` — the profile export had stringified complex values rather than capturing them. There is nothing the generator can emit to satisfy that, so the gate now names it as an unusable vocabulary and points at `Export-WorkshopTelemetryProfiles.ps1`, rather than scoring it as a defect. `AlertInfo` was failing on `Title` and `DetectionSource`, which hold the workshop's own alert catalog and are entity-like by nature; the entity exemption now covers `title`, `source`, `target` and `query`.
- Verified end to end: 622,635 rows across 79 tables, quality gate green at a 94.4% mean with all 69 profiled tables above the bar, identity invariants holding, all smoke tests passing, 40/40 dashboard tiles returning data, and dashboard parity clean. Also fixed the smoke suite's attacker-infrastructure query, which grouped by a `Country` column that `SigninLogs` does not have — the geolocation is in `Location` — and whose helper had been swallowing the error and printing "(no rows)".

### 2026-07-26 — gave the risk detections something to corroborate them

- Fixed a 100% authentication success rate across 30,000 sign-ins. Every sign-in table wrote `ResultType = '0'` unconditionally, which is not a cosmetic gap: `AADUserRiskEvents` raised 782 `passwordSpray` and 740 `leakedCredentials` detections with nothing behind them, so a student following the first move of any credential investigation — pivot from the detection to the sign-in logs — found a spray with a perfect success rate and no failures. The dashboard's failed-login tiles were empty for the same reason.
- Took the ambient failure weights from the tenant profiles rather than inventing them. `SigninLogs` now carries `50140`, `90094` and `50206`; `AADNonInteractiveUserSignInLogs` carries `50011`, `65002` and `50177`; `EntraIdSignInEvents` carries `70043` at the 15.3% the profile records, plus `500011`, `53003`, `9010010` and others. The generator had been violating the repository's own "never invent a vocabulary" rule in both directions — ignoring the failure codes the profiles do record, while `AADManagedIdentitySignInLogs` wrote the literal strings `'Success'` and `'Failure'` into a column that carries an Entra error code, so a query written against real telemetry found nothing there. Service principal and managed identity sign-ins now fail with documented codes (`7000215`, `7000222`, `700027`, `500011`, `500014`).
- Made the spray and the leaked credentials corroborable rather than merely present. A deterministic plan, built from the identity list before any row is written, decides which accounts were sprayed and whose credentials leaked; both the sign-in tables and the risk detections read it, so the two agree regardless of which runs first. Sprayed accounts show `50126` bursts from three attacker addresses, a quarter of them hitting smart lockout (`50053`), and one in twenty-five falling to it — which is the pivot from noisy failures to a foothold. Leaked-credential accounts show the MFA refusal (`50074`) instead, because the password was correct.
- The first attempt at this failed silently and is worth recording. It filtered *ambient* sign-ins by the campaign window, and a 140-minute window catches a fraction of a percent of rows spread over a month: 456 detections, zero corroborating failures — the same unevidenced-detection defect, reintroduced by a different mechanism. Evidence a student is told to go and find has to be written on purpose. `leakedCredentials` had the identical flaw (268 detections, 2 failures) and was caught only because the spray fix prompted the same check.
- Verified in the loaded database: 631,208 rows across 79 tables, `SigninLogs` at 94.03% success and 5.96% failure, and **zero** `passwordSpray` or `leakedCredentials` detections without corroborating sign-in failures. The campaign reads correctly from either view — three attacker IPs, 78 to 160 attempts each, spread across ~40 accounts apiece in both `SigninLogs` and the XDR `EntraIdSignInEvents`.

### 2026-07-26 — fixed four dashboard tiles that were quietly wrong

- Repaired the `has` operator misuse that left two tiles empty and two more silently wrong. `has` matches whole terms, so `ActionType has 'Failed'` never matched `LogonFailed` — a single token — and 649 real endpoint and identity failures were dropped. "Failed logins by source" and "Top failed principals" were empty; "Login outcomes over time" classified every endpoint logon as a success; the "Failed logins" card read 90 instead of 739; and "Successful logins" counted failures as successes, because `!has 'Failed'` is always true. Separately, `OSPlatform has 'Windows'` matched only the 20 devices whose platform is literally `Windows`, bucketing 5,364 real Windows devices as "Other".
- Left the generator alone for the OS case, deliberately. `Windows11`, `WindowsServer2025` and `Windows11WVD` are the values Defender XDR actually emits, and the field profile records them at the observed weights — including the rare bare `Windows` at 0.15%. The data was right and the derivation was wrong; flattening the platform to satisfy a broken query would also have broken any exercise that pivots on a specific build.
- Re-anchored the two compromised-user tiles. Both took `top 1 by TimeGenerated asc` across every risky sign-in, which resolved to the scenario user only because the estate contained exactly one risky sign-in. Once the password spray added realistic risky sign-ins that occur earlier, the anchor moved to an attacker address with no consent or Graph activity behind it and both tiles went blank. They now anchor on the compromised account by name, as the sibling endpoint-activity tile already did.
- Added `scripts/Test-WorkshopDashboardParity.ps1` and `scripts/Sync-WorkshopStudentDashboard.ps1`. The parity check regenerates the dashboard into a temporary directory and compares it to what is committed, then compares the student handout tile by tile. The sync script makes the handout's query text a derived artifact: it is schema 76 where the generator emits schema 20, so it had always been hand-maintained, and it was still carrying seventeen stale fragments after the fixes above — meaning students would have imported the broken tiles the instructor had already fixed.
- Both scripts matched tiles by position at first, and both were wrong. The two files hold the same forty tiles in different order — "Compromised User - Endpoint Activity" is tile 24 in the generated dashboard and tile 39 in the handout — so a positional sync would have written each query onto the wrong tile. The sync script refused rather than proceeding, which is how the assumption was caught; both now match on tile id, which corresponds exactly because both derive from the same deterministic generator.
- All forty tiles return data against the regenerated dataset, and the parity check passes across the `.kql`, both generated dashboards, and both student handouts.

### 2026-07-26 — closed the lexer gap the tests had been documenting (H1c)

- Taught the gateway's statement lexer about backslash escapes and `@`-verbatim literals, closing a bypass that let a crafted string hide a second statement. Given `print x = "\""; .drop table T`, Kusto reads the literal as a single `"` followed by a second statement, while a lexer that models only `''`/`""` doubling sees the quote close and immediately re-open — swallowing the `;` and the `.drop` into what it believes is one `print` statement, which then passed validation. Non-verbatim literals now honour `\` as an escape, `@"..."` and `@'...'` (including the `h@'...'` obfuscated form) treat a backslash as data, and a doubled quote inside a verbatim literal stays inside it.
- Made an unterminated literal or comment a refusal rather than a guess. If the lexer cannot tell where a statement ends, neither can the policy, so the request is rejected instead of forwarded on an assumption. Over-splitting was already the safe direction — an unexpected extra statement is refused — and this keeps every remaining ambiguity on that side.
- The existing test for this had been asserting the *vulnerable* behaviour. `rejects a statement hidden inside a string from reaching Kusto` asserted `allowed: true` for the smuggling payload, with a comment explaining that only the raw-text deny scan caught the subset of payloads containing a denied primitive — so `.drop table T`, which contains none, got through. The test now asserts the statement is seen and refused on its own merits. A test that documents a gap is worth having; one that outlives the gap is worth updating.
- Seven new tests cover the reported payload in both quote styles, verbatim backslash handling, doubled quotes, the `h@'...'` form, all four unterminated cases, and that ordinary multi-statement splitting still works. Suite is 63 tests.

### 2026-07-26 — unpinned the emulator, deliberately

- Reversed the digest pin added earlier the same day. This instance runs indefinitely rather than for a single event, so it tracks the current emulator and picks up Microsoft's fixes as they ship, rather than freezing on a build that will age. Recorded as a decision, with the trade stated in `compose.yaml` rather than left for someone to rediscover: Kustainer writes its persistent database in the format of the build that created it, and a different build cannot attach it, so when the image moves the snapshot in `data/local-kusto` does not come with it. That is recoverable and rehearsed — the NDJSON payload is what actually restores, via `Import-GeneratedDataToKustainer.ps1`. `KUSTO_IMAGE` still accepts a full reference, so pinning for a specific delivery remains one environment variable away.
- Removing the pin also removed the recreate it introduced. Confirmed with `--dry-run`: `docker compose up -d` now reports the `kusto` container as `Running` rather than `Recreate`, because the resolved image string matches what the container was created with again. The persistent database registration is no longer one `up` away from being destroyed.
- Changed the emulator build from an assertion to an observation in `Test-WorkshopReadiness.ps1`. Asserting a fixed build would have turned every legitimate Microsoft update into a red preflight, which trains people to ignore preflights. The build and product version are now reported, and the gate is the table and row count — which is the check that actually catches a version move, since the symptom is a database that will not attach. The remedy on that check now names the import as the fix for that case too, so the failure is explainable rather than mysterious.

### 2026-07-26 — pinned the emulator, and made the quiet failures loud

- Pinned Kustainer by digest in `compose.yaml` rather than leaving it on `:latest`. *(Reversed later the same day — see the entry above. This instance runs indefinitely, so tracking the current emulator was judged worth more than freezing a rehearsed build.)* The workshop's restore path is proven for exactly one emulator build — a persistent database written by one build cannot be attached by another — so a tag moving between rehearsal and delivery is a data-loss event, not a version bump. The pin was the digest the snapshot was rehearsed, backed up, and restored against (build `1.0.9697.27504`, product `2026.07.20.1506-2629-8bf4dbb-master`). `KUSTO_IMAGE` replaced `KUSTO_IMAGE_TAG`, which could not express a digest at all, because `image:sha256:...` is not a valid reference; that replacement stands.
- **Applying the pin will recreate the `kusto` container on the next `docker compose up`**, confirmed with `--dry-run`. *(Moot once the pin was reversed — the resolved image string matches the running container again, and `--dry-run` now reports `Running`.)* The digest resolves to the image already running, but Compose recreates on a changed config hash regardless, and that destroys the persistent database registration.
- Added a `post-merge` hook that rebuilds the read-only gateway when a pull changes its source, then confirms the policy is live rather than merely healthy. The gateway is a Compose `build:` service, so `docker compose up -d` reuses the last locally built image and never consults the source — which had already put a five-day-old pre-hardening build in front of the class while every health check reported healthy. Attaching the rebuild to the event that causes the drift is more reliable than remembering a flag. Git ignores a `post-merge` exit code, so the hook reports loudly rather than pretending to block; `CDW_SKIP_GATEWAY_REBUILD=1` opts out. Written with LF endings, because a shell hook with CRLF fails as `unexpected end of file` — the same defect the repository had already fixed once for `pre-commit`.
- Decided `.show cluster` stays blocked, and added a test so the decision is not quietly reverted. The pre-hardening gateway allowed every `.show`, so its absence from the allowlist is a deliberate behaviour change rather than an oversight. It is not part of the Azure Data Explorer add-connection handshake, so students connect and query normally; clicking into cluster-level detail returns `403`. `.show cluster principals` is exactly the disclosure the allowlist exists to stop. Suite is 56 tests.
- Added `scripts/Test-WorkshopReadiness.ps1`, a single go/no-go check before a class. It covers the six things that are quiet when they break: the four containers, whether the emulator is the rehearsed build, whether the gateway is running its own source rather than a stale image, the table and row counts, the network boundary, and the student path. Every one of those failed at least once during preparation while some other signal said the workshop was fine. The isolation rule is reapplied rather than merely reported, because it lives in the host firewall and does not survive a Docker engine restart; everything else is reported with the command that fixes it, since rebuilding the gateway or re-importing the database are deliberate acts. Verified in both directions — removing the isolation rule turns the verdict red with the exact remedy, and a normal run repairs it and turns it green.

### 2026-07-26 — closed the path around the gateway, and proved it closed

- Added [`scripts/Set-WorkshopNetworkIsolation.ps1`](scripts/Set-WorkshopNetworkIsolation.ps1), which stops the Cloudflare connector reaching Kustainer directly. The `edge`/`backend` split stops `kusto` resolving for the connector, but not an ingress rule naming the backend IP. The rule is one `DOCKER-USER` entry — the chain Docker evaluates before its own accept rules and never rewrites — dropping packets that arrive on the edge bridge and leave on the backend bridge. Applying it changed nothing for the class: verified over the live tunnel afterwards at 79 tables and 623,832 rows, with the ADX web UI handshake and the scenario queries unaffected.
- Corrected the diagnosis recorded earlier the same day. The first explanation blamed `DOCKER-FORWARD` accepting traffic from every bridge. That was wrong, and testing two freshly created networks disproved it — they could not reach each other at all. Docker **does** isolate bridges (`! -i br-X -o br-X -j DROP` in the `DOCKER` chain). What defeats it is **publishing a port**, which inserts an accept above those drops:
  `-A DOCKER -d <kusto-ip> ! -i br-<backend> -o br-<backend> -p tcp --dport 8080 -j ACCEPT`.
  That accept is not restricted by source network, so every container on every other bridge could reach the engine. Binding the host side to `127.0.0.1` constrains the host, not other containers. The mitigation is unchanged; the reason it was needed is now stated accurately in `compose.yaml`, the infra README, and the IaC guide.
- Made the rule survive the topology changing under it. Docker names a bridge after the network ID, so any `docker compose down` yields new bridge names and leaves the old rule pointing at a dead interface — still present in `iptables -S`, protecting nothing. The script reads the edge and backend networks from the running containers instead of hard-coded names, derives bridges from live network IDs, and clears rules of its own shape whose interfaces no longer exist before applying the current pair. It refuses to act if the two networks resolve to the same bridge, and refuses if the connector and the emulator share a network, where no firewall rule could help.
- Added [`scripts/Test-WorkshopNetworkIsolation.ps1`](scripts/Test-WorkshopNetworkIsolation.ps1), which sends packets rather than reading firewall rules, because a stale rule reads as correct. It asserts Kustainer is unreachable from the edge network by name and by raw IP, still reachable from the backend network so the gateway works, that the gateway is still reachable from the edge network so students get in, and that `127.0.0.1:8080` still answers so the instructor and the import scripts are unaffected. Run against the unprotected stack it failed on exactly one assertion — the raw-IP bypass — and passed the other five, which is the behaviour that makes it worth trusting.
- Gave the test a `-SelfTest` mode and put it in CI (`.github/workflows/network-isolation.yml`). It builds two throwaway networks and a listener that publishes a port, then asserts reachable, blocked after applying, still reachable from its own network, and reachable again after `-Remove`. It needs no workshop stack, and the job fails if any rule is left behind. Writing it caught two defects in the test itself before it caught anything else: a `nc -l` listener that served one connection and left gaps reading as "blocked", and a listener with no published port, which was already isolated and so proved nothing.
- Wired both into `Start-CloudflareAdxTunnel.ps1 -Apply`, which now applies the rule and runs the boundary test after the connector starts, failing the run if the connector can still reach Kustainer directly. `-SkipNetworkIsolation` opts out. This is the right place because the rule must be reapplied whenever the networks are recreated, and this script is what brings the class route up. Terraform cannot own it: it is host firewall state inside the Docker VM, not a Cloudflare or Compose resource.

### 2026-07-26 — the network split does less than it claimed

- Corrected a security claim the repository could not support. `compose.yaml` and the entry below stated that splitting `edge` from `backend` meant `cloudflared` "cannot reach `kusto:8080` no matter what the dashboard-managed ingress rule is set to", making the bypass "architecturally impossible". Tested from a container attached to `edge` only — the same network position as the connector — the result was: `kusto` does **not** resolve, `kusto:8080` by name is blocked, but `172.19.0.2:8080` by raw IP is reachable and `GET /v1/rest/ping` against the engine answered. The separation blocks the realistic misconfiguration (a hostname in an ingress rule), not a deliberate one (an IP).
- The cause is a Docker change, not a Compose mistake. On Docker 29.6.2 the old `DOCKER-ISOLATION-STAGE-1`/`-2` chains are gone. *(Corrected later the same day: the mechanism named here — `DOCKER-FORWARD` accepting every bridge — was wrong. Docker does isolate bridges; publishing a port inserts an accept above that isolation. See the entry above.)* Only `internal: true` produces a drop, which was confirmed on a throwaway network — and confirmed to also disable published host ports, so it cannot be applied to `backend` without taking away the `127.0.0.1:8080` the instructor and `Import-GeneratedDataToKustainer.ps1` both depend on. A `DOCKER-USER` rule pinned to the two bridge interfaces is therefore the mitigation, and it is host state rather than repository state.
- What actually enforces the boundary is therefore the read-only gateway plus the Cloudflare ingress configuration and Access policy, not the network topology on its own. *(Now backed by `Set-WorkshopNetworkIsolation.ps1`; see the entry above.)* A control that is documented accurately is worth more than one that is documented confidently.

### 2026-07-26 — the hardened gateway locked out the client it exists to serve

- Fixed the database allowlist rejecting the Azure Data Explorer web UI's opening calls, which made the workshop unreachable from `https://dataexplorer.azure.com` entirely. **Add connection** reported only `Request failed with status code 403`. The allowlist required every request to name a permitted database, but the web UI's first management calls deliberately carry no database — discovering which databases exist is what they are for. Every database-scoped call worked, so the failure appeared only at the one step every student must complete first. A request naming no database is now permitted for cluster-scoped discovery (`.show version`, `.show databases`, `.show databases schema`, `.show schema`) and only on the management endpoint; a database-less *query* would run against the engine's default database, which is what the allowlist exists to prevent, and is still refused.
- Forwarded `/v1/rest/auth/metadata` instead of answering `404`. The web UI fetches it before it will talk to a cluster at all; it is how the browser learns the local emulator wants no Entra sign-in. Kustainer answers `200` with a body carrying no data, so there was nothing to withhold.
- The gap was in the testing, not the reasoning. The gateway's own suite and every probe run against it supplied an explicit database, so the database-less path — the one path the real client always takes — was never exercised. Three tests now cover it: the full handshake succeeds, the auth-metadata probe is forwarded and rejects non-GET, and a database-less request is still refused for row data, for non-discovery management commands, and for a whitespace-only database name. Suite is 55 tests.
- Diagnosed from the gateway's own structured audit log, which recorded `{"outcome":"blocked-database","db":null,"status":403}` against the browser's requests. That log was added in the hardening pass for exactly this purpose and paid for itself the first time it was needed.

### 2026-07-26 — the hardened gateway was not the gateway that was running

- Documented that the read-only gateway must be rebuilt explicitly, because a stale build had been serving the class. The gateway is a Compose `build:` service, so `docker compose up --detach` reuses whatever image was last built on the host and never consults the source. After the gateway hardening landed, bringing the stack up on the new Compose definition left a container running an image built five days earlier, from before the hardening commit: `.show queries` — the disclosure command the allowlist exists to stop, and the one named in the allowlist's own comment — was answered `200`. The source was correct, the tests were green, and the policy was not in force.
- The verification that should have caught this reported success instead. `docker compose ps` said `healthy` and the container health check passed, because the health check only proves the process answers on `/healthz`; neither can distinguish a current policy build from a stale one. Two of the three denials observed against the stale image were also not the gateway's doing — they carried Kusto's `400`, not the gateway's `403` — so a check that only recorded status codes read as enforcement where there was none. Refusals are now attributed to their source before they are counted as evidence.
- The instructor checklist step that read "confirm `kusto-readonly-gateway` is healthy" now rebuilds it first with `docker compose up --detach --build kusto-readonly-gateway` and confirms the policy itself: `.show tables` must succeed and `.show queries` must return `403`. The same warning is in the README's gateway section. Rebuilding the gateway alone does not disturb the `kusto` container or the persistent Student snapshot.
- Verified after the rebuild: all 52 gateway tests pass on the Node version the container actually ships, and every one of ten allow/deny cases behaves correctly with each refusal attributed to the gateway rather than to Kusto.

### 2026-07-26 — repository evaluation follow-up

Work from the "Suggested order of work" in `REPO-EVALUATION-2026-07-26.md`, taken in value-per-effort order. Every change below was verified where a runner exists: the gateway suite runs green (52 tests), all PowerShell files parse cleanly and lint with zero error-severity findings, `Test-WorkshopPackage.ps1` passes on a clean clone, `Test-RepositorySafety.ps1` was exercised against a fixture repo, and `compose.yaml` and every workflow validate.

Gateway (`tools/kusto-readonly-gateway/server.mjs`):

- Stopped one HTTP request from killing the class gateway. A request body of the four bytes `null` parsed successfully and then dereferenced `null.csl` inside an `async` handler with no `catch`, which Node turns into a process-ending unhandled rejection — under `restart: unless-stopped` a single repeated `curl` became a restart loop, and every student has a terminal open. The handler body is now wrapped so every failure path answers with a status instead of crashing, non-object JSON bodies (`null`, `true`, `42`) are rejected with `400`, and `process.on('unhandledRejection')` and `process.on('uncaughtException')` are installed as a second layer. This was the single highest-value change in the repository.
- Closed the validate-one-thing, forward-another bypass. The gateway validated the `csl` it parsed with V8 but then forwarded the client's raw bytes, so any parser disagreement between V8 and Kustainer's .NET deserializer — duplicate keys, key casing, escaped key names — let a body like `{"csl":".show tables","Csl":".drop table X"}` read as harmless in the gateway and execute as the drop downstream. The forwarded body is now rebuilt from the validated fields (`csl`, `db`, an allowlisted subset of `properties.Options`) so the gateway forwards exactly what it checked.
- Forced read-only request options server-side. `properties` was forwarded untouched, so a student could set `notruncation: true` and an unbounded `servertimeout`. The rebuild drops every `Options` key that is not allowlisted, clamps `servertimeout` to `KUSTO_MAX_SERVER_TIMEOUT_SECONDS` (default 240), forces `query_language = 'kql'`, and — unless `KUSTO_FORCE_READONLY=false` — sets `request_readonly = true`. The escape hatch exists because Kustainer builds vary in whether they honour that option; it defaults on.
- Rejected the query-language egress and code-execution primitives that never start with a dot. "Does not start with `.`" is not the same as read-only: `externaldata`, `evaluate python/R`, the `http_request`/`sql_request` family, and `cluster(...)` all reach outside the database. They are now denied on the raw request text, so a primitive hidden inside a string literal is rejected too — over-blocking there fails closed, which is the correct direction for a policy boundary.
- Replaced the blanket `.show` allow with a metadata-read allowlist. `.show` is read-only for data but not for secrets: `.show queries` and `.show commands-and-queries` echo other students' query text, `.show journal` is the full command history, and on the managed ADX path `.show ... principals` discloses real tenant identities. Only `.show version`, `.show schema`, `.show databases`, `.show databases schema`, `.show tables`, `.show table <t> schema`/`cslschema`, and `.show functions`/`.show function <f>` are permitted.
- Added a database allowlist. `payload.db` was never inspected, so a student could target `$systemdb` or `KustoMonitoringPersistentDatabase`. `KUSTO_ALLOWED_DATABASES` (set to `CyberDefendStudentSnapshot` in Compose) is now checked before forwarding; unset means no restriction.
- Added rate limiting and a concurrency cap. Nothing bounded requests or query cost, so one `range i from 1 to 10000000000 | ...` could wedge the shared 4-CPU Kustainer for the whole room. Each source address now gets a token bucket (`KUSTO_RATE_BURST`, `KUSTO_RATE_PER_MINUTE`) and in-flight upstream queries are capped (`KUSTO_MAX_IN_FLIGHT`, default 8); over-budget clients get `429` with `Retry-After`.
- Added a structured audit log. The gateway now emits one JSON line per decision (`forwarded`, `blocked-kql`, `blocked-database`, `rate-limited`, `at-capacity`, `unknown-route`, `handler-error`), so a class incident is answerable from `docker compose logs` instead of guesswork — and in a cyber-defense workshop that log is itself a teaching artifact.
- Fixed the oversized-body path so the client actually receives the `413`. The body reader destroyed the socket, which raced the response into a connection reset; it now pauses the request, writes the `413`, and closes the connection after the reply is flushed.

Gateway tests and container:

- Expanded `server.test.mjs` from 9 tests to 52, covering the null-body crash, the canonical-forwarding rebuild, the `.show` allowlist (allowed and disclosure commands), the egress-primitive denials, the database allowlist, the rate limiter and in-flight cap, and the `403`/`404`/`405`/`413` error paths that were entirely untested. `npm test` runs green.
- Hardened the Dockerfile (`M8`): pinned the base to an immutable patch tag instead of the moving `node:24-alpine`, added `USER node` so the internet-facing boundary no longer runs as UID 0, added a `HEALTHCHECK` and `ENV NODE_ENV=production`, and paired it with Compose `init: true` so `node` as PID 1 no longer stalls 10s on every `docker compose down`. Added `.dockerignore` and a `package-lock.json` so the build context is minimal and `npm ci` is possible.

Compose (`compose.yaml`):

- Split the flat `adx-internal` network into `edge` (cloudflared + gateway) and `backend` (gateway + kusto), so `kusto` no longer resolves for `cloudflared` and a hostname-based tunnel ingress rule cannot reach the emulator. *(Corrected 2026-07-26: this entry originally claimed the connector "cannot reach `kusto:8080` no matter what the dashboard-managed tunnel ingress says" and that the bypass was "architecturally impossible". Testing disproved that — see the network isolation entry above. The split blocks the realistic misconfiguration, not a deliberate one.)*
- Pinned Kustainer's image via `KUSTO_IMAGE_TAG` instead of a hard `:latest`, so a rehearsed emulator version can be locked for delivery day; added `security_opt: [no-new-privileges:true]`, `init: true`, and JSON log size limits to every service; and wired the read-only and database-allowlist env vars into the gateway service.

PowerShell:

- Passed a single `-TelemetryEndTime` from the parallel driver to every worker (`H5`). The generator defaults it to `(Get-Date)` and anchors every ambient row to it, so staggered worker starts anchored each process to a different "now" and made reruns non-reproducible — contradicting the driver's own docstring. One captured value is now shared by all workers.
- Stopped passing the Cloudflare Service Token secret as a command-line argument in `Start-StudentAdxProxy.ps1` (`H10`). Command-line arguments are visible in the process table to any other local user, and this runs on student laptops. The script already read `TUNNEL_SERVICE_TOKEN_SECRET`; it now sets that env var and drops the `--service-token-secret` flag, clearing it in a `finally`.
- Bounded and made resilient the module's one ADX chokepoint (`M3`). `Invoke-WorkshopAdxManagementCommand` called `Invoke-RestMethod` with no client timeout and no retry, so a hung connection hung the script forever and one transient `429`/`503` during ingest failed the whole run; it now uses `-TimeoutSec 300 -MaximumRetryCount 3 -RetryIntervalSec 5`. `Get-WorkshopAdxAccessToken` now caches the token to its real expiry instead of acquiring a fresh one on every call, which removed dozens of redundant token acquisitions per batched ingest.
- Checked `$LASTEXITCODE` in `Get-WorkshopAdxAccessToken` (`M4`). The Azure CLI path ignored the exit code, so garbage stdout could be returned as a bearer token and surface as a confusing 401 far downstream instead of a clear "run az login" at the source.
- Added `Get-MgUser` to the preflight in `New-WorkshopStudents.ps1` (`L12`). It is called in the per-student loop but was missing from the required-command check, so the script passed preflight and failed mid-loop, after it had already created the group.
- Fixed `Join-Path` path separators across 25 files (`M1`). `Join-Path $base '..\schemas'` yields a literal backslash in the path on Linux, so `Test-Path` fails; every `Join-Path` with an embedded backslash was rewritten to the multi-segment idiom already used correctly in `Test-FieldProfileSafety.ps1`. This is the prerequisite that lets any PowerShell gate run on `ubuntu-latest`; all files still parse cleanly.
- Made `Test-WorkshopPackage.ps1` pass on a clean clone (`H4`). The real-export CSV header checks require `sample/*-Real.csv`, which `.gitignore` excludes by design, so the repository's broadest validator failed immediately for anyone but the author and was never in CI. Those checks now sit behind a `-WithRealSamples` switch, and a missing default `data/generated` is a skip rather than an error, so the parser, schema, and manifest checks — the bulk of the validator — run in CI. Verified: exit 0 on a clone with no `sample/` and no generated data.

Telemetry-safety parity (`M9`):

- Added `scripts/Test-RepositorySafety.ps1` as the single enforcement point that both `.githooks/pre-commit` and `.github/workflows/telemetry-safety.yml` call, so the hook and CI can no longer drift apart. It enforces the forbidden-path rules — nothing under `sample/`, no `students/*.csv` (rosters carry passwords and TAP values), no `samples/*.csv`, nothing under `data/generated/` — and runs the field-profile tenant scan. In `-Staged` mode (the hook) it materializes the staged blob contents and scans those, closing the "git add, then edit the file" gap where the working tree differed from what was committed. The hook previously did not check `students/` at all. Verified against a fixture repo: clean tree passes, a staged `students/*.csv` is blocked, a staged profile carrying a subscription GUID is blocked, and a clean-staged / dirty-worktree profile passes while its dirty-staged version is blocked.

CI:

- Added `gateway-tests.yml` (Node 24, `npm ci` + `npm test`, path-filtered) so the one security-critical component finally runs in CI; `powershell-lint.yml` (PSScriptAnalyzer over `scripts/`, `tools/`, `adx_db_backupNrestore/`, failing only on error severity and surfacing warnings as annotations, so it is green today while exposing the documented style debt); `iac-validate.yml` (`az bicep build`/`lint` and `terraform fmt`/`init -backend=false`/`validate`); and `package-validation.yml` (the clean-clone static half of `Test-WorkshopPackage.ps1`). Applied the cross-cutting edits to every workflow including the existing one: SHA-pinned `actions/checkout`, `concurrency` with `cancel-in-progress`, and `timeout-minutes`.

Repository governance (§3.7):

- Added the governance files that were simply absent: `LICENSE` (MIT — without it nobody could legally reuse a workshop built to be reused), `.gitattributes` (normalizes line endings so the `/bin/sh` hook and the whole-repo CRLF↔LF diff stop being cross-platform hazards), `SECURITY.md` (private reporting, scope, and the by-design non-defects), and `.github/dependabot.yml` (weekly bumps for the SHA-pinned Actions, the gateway's npm and Docker base).

- Fixed a silent data loss defect in `scripts/Backup-LocalKustoSnapshot.ps1`. The script chose its restorable payload by taking the most recently written directory under `data/local-export`, which meant every archive it produced shipped the 48-table, 358,621-row copy left behind by the last Azure Data Explorer sync rather than the 79-table, 623,832-row database it was supposedly backing up. Nothing reported a problem, because the script never compared the payload to the database or to the manifest. The payload is now taken from `data/generated`, which is the current source of truth, and the script refuses to produce an archive unless the payload covers every table in `metadata/tables.manifest.json`. Pass `-UseLocalExport` to select an Azure Data Explorer export deliberately, or `-AllowIncompleteExport` to accept a partial archive knowingly.
- Made backup archives self-contained. They now carry the table schemas and manifest alongside the data, so restoring no longer depends on a matching checkout being present. The backup manifest records the payload source and both the exported and declared table counts.
- Stopped the restore script reporting work it had not done. In extract mode it announced the container name and port it was about to use, and finished with "Restore container and working files removed", while starting and removing nothing. Extract mode now invokes no external command at all, and the container options moved into their own parameter set so `-KeepContainer`, `-ContainerName`, `-HostPort`, `-Memory`, and `-Image` are rejected outright rather than silently ignored when combined with `-ExtractPayloadTo`. Misleading output and silently discarded options are both worth treating as defects in a tool someone reaches for when the workshop is already broken.
- Documented restoring everywhere the repository tells you to take a backup. Five documents instructed the reader to create an archive and none said how to use one, and the container-replacement guidance routed recovery through `Copy-StudentAdxToLocalKusto.ps1 -ForceRecreate`, which re-copies from the Student cluster in Azure. That path needs cluster access and an entitled sign-in, so the documented recovery was unavailable in precisely the situation it existed for: a venue with no usable connectivity, or a cluster that no longer exists. The backup was written and then abandoned. `Restore-LocalKustoSnapshot.ps1 -ExtractPayloadTo` now puts the archive's telemetry and schemas back on disk for `Import-GeneratedDataToKustainer.ps1` to load, giving an offline rebuild, and that route is documented in the README, the Cloudflare access guide, the instructor guide, the Cloudflare infrastructure README, and the IaC setup guide.
- Added `scripts/Restore-LocalKustoSnapshot.ps1`, so restoring is a repeatable operation rather than something reconstructed by hand under pressure. It rebuilds the database in a throwaway container on a spare port, reconciles the restored row count against the archive, refuses to run against the workshop container, and cleans up after itself. Verified against the current archive: 79 of 79 tables and 623,832 of 623,832 rows, table-for-table identical to the live cluster, with the workshop's credential access queries returning the expected results.
- Recorded why the NDJSON payload is what makes an archive restorable. The emulator registers a persistent database inside the container rather than in the mounted state directory, so a fresh container cannot attach persisted state another container wrote; `.create database ... persist` fails with an internal service error and `.attach database` reports a missing V11 pointer. The persisted state in an archive is therefore only useful for restoring files under the same container instance, and structural validation of the ZIP proves nothing about whether it can be restored.
- Made the tenant data safeguard enforced rather than advisory. `metadata/field-profiles/` is tracked because those profiles are what make the generated telemetry reproducible, which leaves them as the one path by which live tenant data could reach a public repository. A tracked `.githooks/pre-commit` now blocks any commit that stages raw capture data from `sample/`, and runs the profile scan whenever a field profile is staged; `scripts/Install-WorkshopGitHooks.ps1` activates it for a clone, since Git does not version `.git/hooks`. Because a local hook can be skipped with `--no-verify` and does nothing until installed, `.github/workflows/telemetry-safety.yml` runs the same scan on every push and pull request, and additionally fails if raw telemetry or the generated package is ever tracked, or if the manifest, schemas, and profile overrides disagree.
- Fixed `scripts/Test-FieldProfileSafety.ps1`, which had an absolute path to one machine and so could not run for anyone else or in CI. It now resolves the profile directory relative to the script, accepts `-ProfileDirectory` and `-Quiet`, and reports which columns failed and how to regenerate them.
- Stopped tracking `data/generated/`. The package is roughly 900 MB of NDJSON and is now reproduced deterministically from the committed schemas, field profiles, and fixed random seed, so the generator is the artifact rather than its output. `DeviceProcessEvents.json` had reached 95.7 MB against GitHub's 100 MB hard limit, and every column added to that table moved it closer. `data/scenario-summary.json` stays tracked because it documents the scenario rather than carrying bulk data.
- Documented the offline build path in the README: generate, import to Kustainer, then run the package, quality, and identity gates. Also documented how to refresh the field profiles and the requirement that `Test-FieldProfileSafety.ps1` reports clean before profiles are committed.
- Corrected the expected total row count in the student lab setup guide from roughly 629,000 to roughly 624,000 to match the regenerated package.
- Added `scripts/Import-GeneratedDataToKustainer.ps1`, which creates each table from its schema, applies a JSON ingestion mapping, clears existing rows, ingests the generated NDJSON from the read-only mount, and reconciles ingested row counts against the files on disk. It talks to the emulator's REST endpoint directly because the local cluster is unauthenticated, whereas the shared management helper always demands an Azure bearer token.
- Made ambient `DeviceNetworkInfo` rows agree with the per-device inventory records. Both now draw from the same adapter catalog and build the hardware address from the vendor's own IEEE OUI, so identifying hardware from the first three octets always matches the reported vendor. Previously the two paths used different vendor lists, which produced adapters whose OUI contradicted their vendor.
- Replaced the placeholder tenant and subscription identifiers with real random GUIDs. One tenant GUID now backs every table that carries a tenant column, and Azure resources are spread across a fixed pool of 25 subscriptions selected by hashing the resource, so a device or storage account keeps the same subscription across every table that references it. Added `scripts/Test-WorkshopIdentityInvariants.ps1` to enforce both rules; the all-zero GUID is tolerated as a well-known management group and Microsoft Graph placeholder.
- Filled the gaps that made several tables unusable, using real product vocabularies rather than invented values:
  - `DeviceNetworkInfo` now carries a complete adapter record. Vendors are real NIC manufacturers paired with their actual IEEE OUI prefixes, and the MAC address is built from that prefix so hardware identification from the first three octets agrees with the reported vendor. Gateways and DHCP servers derive from the device subnet, and DNS resolvers follow the site. Virtual hosts get hypervisor adapters and physical hosts get real silicon. This table is written one row per device by the inventory loop, not by the ambient branch, which is why it had stayed empty.
  - Endpoint tables now report file version metadata for known binaries. Signed Microsoft binaries carry their shipping resource strings, and the credential access tooling in the scenario carries its real authorship, which is the pivot that separates it from living-off-the-land activity.
  - `AlertInfo` and `AlertEvidence` ambient alerts now carry real Microsoft Defender and Microsoft Sentinel titles with the MITRE ATT&CK tactic and technique identifiers each maps to. `AttackTechniques` had been hard-coded empty, which made technique-based hunting impossible.
  - `DeviceTvmSecureConfigurationAssessmentKB` and `DeviceBaselineComplianceAssessmentKB` are driven by a catalog of real Microsoft, CIS, and DISA STIG security configuration entries with rationale, risk description, CCE reference, and remediation steps.
  - `AppGenAIContent`, `IntuneDevices`, and `CloudAppEvents` gained generator branches. `AppGenAIContent` emits OpenTelemetry gen_ai spans for the same agents declared in `AgentsInfo`, so a hunt can pivot from an agent registration to what the agent actually did.
- Recorded the columns the workshop deliberately populates even though the source tenant leaves them empty in `metadata/profile-overrides.json`, covering agent governance, directory enrichment on the identity tables, and Azure resource association on `DeviceInfo`. The generator and the quality gate both read that file.
- Fixed three latent defects in the generator: a comma-precedence bug that made an arithmetic expression operate on an array, a hardware address format that produced malformed octets past device 255, and an `[array]::IndexOf` lookup that rescanned the whole fleet on every iteration.
- Stopped field profiles from carrying tenant data. Suppressing identity and free-text columns by name missed columns whose values embed tenant identifiers, such as `SourceAgentId`, which carries Azure resource IDs containing a live subscription GUID; a scan of the committed profiles found 70 such columns. Captured vocabularies are now dropped when any value contains an embedded GUID, a subscription, resource group or tenant path, a UPN, an address, a bearer token or JWT, or a long hex string. Version columns are exempt from the address test so product versions are not mistaken for an IP. `scripts/Test-FieldProfileSafety.ps1` enforces this and now reports clean, with 762 of the original 819 vocabularies retained.
- Made the generator consume the real field profiles: columns that production populates but that carry no safe vocabulary now receive a shape-matching synthetic value at the observed fill rate, and columns empty in production are cleared on ambient rows unless listed in the new `metadata/profile-overrides.json`, which the generator and the quality gate both read.
- Removed the fallback record that seeded every table lacking scenario evidence with a fabricated `WorkshopBaseline` action and application plus endpoint and identity columns most tables do not declare. Every table now reaches its row count through the normal generator.
- Rebuilt `AgentsInfo`, which had no generator branch and emitted 32 of its 39 columns empty. It now generates a declared-agent inventory with model, endpoint, MCP server, skill, permission, guardrail, trigger, data source, and owner detail, with instruction and tool fill rates tracking the real inventory at 52 and 40 percent.
- Added `scripts/Test-SyntheticDataQuality.ps1`, which scores generated tables against the real field profiles and reports sparse, overfull, and out-of-vocabulary columns.
- Added `scripts/Export-WorkshopTelemetryProfiles.ps1`, which derives per-column field profiles for every workshop table from real telemetry so the synthetic generator can reproduce production shape instead of inventing values. Rows are merged from three sources: the curated captures in `sample/`, Log Analytics workspace `DIBSecCom`, and Defender XDR advanced hunting for the tables that exist only in XDR. Profiles record fill rate, cardinality, weighted value vocabulary, detected format pattern, and numeric or length bounds, and they explicitly mark columns that are empty in production so the generator can leave them empty by design rather than by oversight.
- Generated field profiles for 69 of the 79 tables from real telemetry. The remaining 10 emit no data anywhere in the tenant and are driven from their documented Microsoft Learn schemas instead.
- Replaced `DeviceTvmBrowserExtensionsKB` with `AppGenAIContent` in the table manifest, schemas, and parallel generator. The new table is grounded in Log Analytics `DIBSecCom` and reconciles exactly with `sample/AppGenAIContent-RealTelemetry.csv` and the published schema at 214 rows and 24 columns. The package remains at 79 tables.

- Rewrote the instructor storyline around Acts 0-12 so it opens on the device-code phish and the benign twin instead of the risky sign-in, and added the closing exfiltration, threat-intel, and XDR incident beats.
- Made `STUDENT-GUIDES/dashboard-CYBER-DEFEND-V4.json` the single authoritative dashboard across the README and instructor guide; the older schema-20 export is marked superseded.
- Refreshed `docs/diagrams.md` prose and re-sequenced all three diagrams for the current scenario: the attack storyline now opens on the device-code phish and closes on cloud exfiltration, the investigation pivots start at the phish and end on exfiltration plus the threat-intel join, and the hybrid topology gets the real `usag-cyber.local` domain, 79 tables, a 5-100 student range, and a student file that still exists.
- Made the student lab setup guide cross-platform end to end: Windows, MacOS, and GNU/Linux in the intro and prerequisites, a `curl` variant for non-Windows, and a PowerShell 7 note for the proxy script.
- Aligned the shared credential lifetime to `720h` (30 days) in Terraform and all documentation, matching the value applied to the live token.
- Added the device-code phish, URL click, token redemption, and benign twin to the generated scenario summary, using the generator's real offsets.
- Added phase and per-file progress output to `Test-WorkshopPackage.ps1`, which previously ran for minutes with no indication it was working.
- Regenerated `docs/conference_abstract.pdf` from the updated abstract.
- Rewrote the conference abstract for the current device-code phishing emulation: thirteen acts, the benign twin, cloud exfiltration, threat-intel correlation, XDR incident grouping, 79 tables, and a fixed 120-minute length.
- Removed `;Fed=false` from every ADX connection instruction across the README, student guide, class guide, IaC setup, gateway, and infra docs; the student connection URI is now `http://127.0.0.1:8080`.
- Added a collapsible GNU/Linux Debian install path to step 1 of the student lab setup guide, using Cloudflare's apt repository with a direct `.deb` fallback.
- Made the MacOS install path in step 1 of the student lab setup guide a collapsible section, added `brew update` before the `cloudflared` install, and added sparing section emojis.
- Split step 1 of the student lab setup guide into Windows (`winget`) and macOS (Homebrew) installation paths, including how to install Homebrew for students who do not have it.
- Corrected deck slide 3 from 60 to 120 minutes and rebalanced its five segments to 10/40/30/35/5, matching the flow diagram; its second segment now reads Acts 2-5 instead of double-counting the orientation acts.
- Renumbered the acts in `docs/instructor_answer_key.kql` onto the deck's Act 0-12 scheme, with answer-key-only material continuing at Acts 13-16, so the key and the deck can be cross-referenced.
- Refreshed the `workshop_design.md` scenario prose and table families for the device-code phish opening, the benign twin, cloud exfiltration, threat-intel correlation, and the 79-table package.
- Re-cut the workshop flow diagram and the `workshop_design.md` agenda around the Act 0-12 scenario, keeping the two-hour total while rebalancing to 10/20/20/30/20/15/5 minutes so the device-code phish, benign twin, cloud exfiltration, and threat-intel beats are visible.
- Added Microsoft Defender for Office 365 and Microsoft Sentinel + Azure panels to the key-tables diagram, and corrected the package table count from 47/48 to the actual 79.
- Replaced the README lab topology diagram with the kill-chain ribbon variant `images/adx-lab-topology-A-ribbon.png`.
- Made the Docker Kustainer, Cloudflare Service Auth, and read-only gateway route the primary conference delivery model; repositioned managed Azure ADX and Microsoft Entra B2B material as the secondary governed option.
- Pinned `kusto-defaultdb-cleaner`, `kusto-readonly-gateway`, and `cloudflared` to 1 GiB memory and 1 GiB swap in Compose and the Terraform-generated override.
- Added `Backup-LocalKustoSnapshot.ps1` to archive the local Kustainer state, recovery directories, and newest verified NDJSON export with a SHA-256 checksum.
- Replaced the hard-coded student Service Token command in the setup guide with the generated, untracked `student-access.env` workflow.
- Clear stale internal `NetDefaultDB` metadata before Kustainer starts so the default-database cleaner no longer causes a restart loop after a normal stop/start cycle.
- Replaced the manual local Kusto and Cloudflared container workflow with Docker Compose, keeping Kusto bound to `127.0.0.1:8080` and routing the tunnel over an internal Compose network.
- Updated the Cloudflare Tunnel launcher to provision the remote resources, create an ignored connector-token environment file, and support a one-time migration from legacy containers.
- Added a shared 48-hour-minimum Cloudflare Service Auth credential and read-only KQL gateway for classroom access without per-student Cloudflare seats.
- Documented `Fed=false` for student Kustainer connection URIs so ADX clients do not attempt Microsoft Entra authentication against the local emulator.
- Added ADX web UI CORS and private-network preflight support to the read-only gateway for browser connections through the local student proxy.
- Updated class, instructor, managed-access, and troubleshooting documentation for the shared 168-hour Service Token, `Fed=false` connection URI, read-only gateway, and ADX browser CORS/private-network recovery path.
- Added a continuous Kustainer cleaner that removes `NetDefaultDB` and its persistent state after the Student snapshot database is available.
- Added a dedicated gateway reference describing its read-only policy, browser support, cleaner lifecycle, configuration, and security boundary.
- Configured Kustainer for a graceful `SIGINT` shutdown so the mounted Student snapshot remains available across normal Compose stop/start cycles.
- Added a source-of-truth workflow that copies the live Student ADX database into a persistent local Kusto emulator and verifies table row counts.
- Increased the Terraform-managed Kustainer profile from 16 GiB to 24 GiB of memory and swap while retaining 4 vCPUs, with a Terraform-generated Compose override and in-place runtime synchronization that preserves the Student snapshot.
- Added a Cloudflare provider v5 Terraform module and secure environment-driven connector launcher for `adx.tier1-cyberdefense.ai`.
- Added browser-authorized Cloudflared DNS routing for restricted API tokens and made tunnel launcher reruns preserve a healthy connector.
