# This file contains no secrets. Copy it to terraform.tfvars and replace the values.
cloudflare_zone_id = "<CLOUDFLARE_ZONE_ID>"
allowed_emails = [
	"dcodev1702@gmail.com",
	"collaborator@example.com"
]

# The default route targets the Kusto TCP service through the internal Docker Compose network.
# For a cloudflared connector running directly on the host, use tcp://127.0.0.1:8080.
# local_service = "tcp://127.0.0.1:8080"